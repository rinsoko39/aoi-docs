# 2.4 语句块和表达式块

块是用大括号包围的若干内容。块中声明的变量作用域仅限块内。

## 语句块

语句块是用大括号包围的若干语句。语句块是一个语句。

例如：

```aoi
{
    let a = 42;
    a += 1;
}
```

在该块外，块内声明的 `a` 将无效。

## 表达式块

相比语句块，表达式块最后的内容是一个表达式。表达式块是一个表达式。它的值是最后的表达式的值。

例如：

```aoi
{
    let a = 42;
    a += 1;
    a
}
```

它的值将是 `43`。同样，在该块外，块内声明的 `a` 将无效。
