# 1.1 值类型和引用类型

Aoi 中存在值类型与引用类型。

## 值类型

值类型仅表示语义上的值类型，而不保证它的实际存储方式。这大部分是由于自身的能力限制：由于 Aoi 借用 .NET CLR 的内存管理（但它又是解释型的，不在 CLR 声明新的类和结构体），所有值不得不被装箱为一个基类。

值类型在赋值时会复制一份内容。此时一方的值改变后另一方并不会改变。

## 引用类型

引用类型在赋值时仅会复制引用。此时修改一方的引用内容另一方也会改变，因为实际的数据只存了一份。
